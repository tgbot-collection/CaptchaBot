version: '3.1'

services:
  redis:
    image: redis:alpine
    restart: always
    logging:
      driver: none

  ytdl:
    image: bennythink/joingroup
    env_file:
      - env/joingroup.env
    restart: always
    depends_on:
      - redis
